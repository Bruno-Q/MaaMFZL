{
  "scripts": {
    "build:python": "python3 -m venv .venv && . .venv/bin/activate && python -m pip install --upgrade pip build && python -m build"
  },
  "devDependencies": {
    "prettier-plugin-multiline-arrays": "^3.0.6"
  }
}
